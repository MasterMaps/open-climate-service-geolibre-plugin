// Build-time constant injected by Vite (`define` in vite.geolibre.config.ts) from
// package.json's version, so the plugin object's version matches the manifest.
declare const __OCS_PLUGIN_VERSION__: string;
